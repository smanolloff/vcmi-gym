#
# XXX: ALWAYS save a lock file when modifying requirements:
#
#     pip3 freeze -r requirements.txt > requirements.lock
#
numpy==1.26.3
pyyaml
ray == 2.9.0
ray[tune] == 2.9.0
wandb == 0.16.6
gymnasium
onnx-pytorch

# VCMI uses CXX11 ABI
# Python's "torch" package is *not* compiled with CXX11 ABI flag by default:
# - on macOS, compatibility between the two is natively provided
# - on linux, the two are incompatible (undefined symbols), so a special cxx11.abi version of torch is needed
torch == 2.1.2; sys_platform != 'linux'
https://download.pytorch.org/whl/cpu-cxx11-abi/torch-2.1.2%2Bcpu.cxx11.abi-cp310-cp310-linux_x86_64.whl; sys.platform == 'linux' ; python_version < '3.11'
https://download.pytorch.org/whl/cpu-cxx11-abi/torch-2.1.2%2Bcpu.cxx11.abi-cp311-cp311-linux_x86_64.whl; sys.platform == 'linux' ; python_version ~= '3.11.0'

