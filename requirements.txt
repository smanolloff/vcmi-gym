#
# XXX: ALWAYS save a lock file when modifying requirements:
#
#     pip3 freeze -r requirements.txt > requirements.lock
#
numpy==1.26.3
pyyaml
ray == 2.9.0
wandb == 0.16.6
gymnasium
torch == 2.1.2; sys_platform != 'linux'
https://download.pytorch.org/whl/cpu-cxx11-abi/torch-2.1.2%2Bcpu.cxx11.abi-cp310-cp310-linux_x86_64.whl; sys.platform == 'linux'
onnx-pytorch
